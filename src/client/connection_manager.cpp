#include "connection_manager.h"
#include <iostream>

ConnectionManager::ConnectionManager() {}

void ConnectionManager::connectToServer(const char* serverAddress, int port) {
    // Connect to server
}
